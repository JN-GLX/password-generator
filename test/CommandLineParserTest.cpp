/*
Tests unitaires à prévoir:
 - Test de validation des arguments
    par exemple si un argument doit être un entier, le test devrait vérifier qu'une exception est levée si une chaîne de caractères est passée à la place.

 - Test de la gestion des arguments manquants
    Si un argument est obligatoire, le test devrait vérifier qu'une exception est levée si cet argument n'est pas fourni.

 - Test de la gestion des arguments en double
    Le test devrait vérifier qu'une exception est levée si un argument est fourni plus d'une fois.

 - Test de la conversion des types d'arguments
    Si un argument doit être converti en nombre entier, le test devrait vérifier que la conversion est effectuée correctement.

 - Test de la gestion des arguments inconnus:
    Le test devrait vérifier qu'une exception est levée si un argument inconnu est fourni.
*/