#include "application.h"


Application::Application() : m_pwdLength(9), m_nbPwd(1), m_argCount(0)
{
    //ctor
}

Application::~Application()
{
    //dtor
}

void Application::readArguments(int argc, char **argv) {
    m_argCount = argc;
    for (i = 0; i < argc; ++i)
    {
        m_argList.push_back(std::)
    }
}
